(ocamllex
 (modules nxnLexer))

(menhir
 (modules nxnParser))

(env
 (dev
  (flags
   (:standard -w -26-27)))) ; use -A to disable all warnings

(library
 (name nxn)
 (preprocess
  (pps
   ppx_deriving.show
   ppx_deriving.map
   ppx_deriving.fold
   ppx_deriving.iter
   visitors.ppx)))
