(ocamllex
 (modules nxnLexer))

(menhir
 (modules nxnParser))

(env
 (dev
  (flags
   (:standard -w -A))))

(library
 (name nxn)
 (preprocess
  (pps ppx_deriving.show ppx_deriving.map ppx_deriving.fold ppx_deriving.iter visitors.ppx)))
