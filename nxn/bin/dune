;; Statically build compiler executable in release mode

(env
 (release
  (flags
   (:standard -linkall -cclib -static -cclib -no-pie))))

(executable
 (public_name nxn)
 (name main)
 (libraries nxn))
