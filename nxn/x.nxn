fn main() int {
    let x = {
        let y = INT_MAX();
        set @if y +>= 0 {
            set y + 1;
        } else {
            set y;
        };
    };
    return 0;
}

fn INT_MAX() int {
    return 0;
}
