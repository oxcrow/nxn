fn main() int {
    let x = {
        let y = INT_MAX();
        set @if y +>= 0 {
            set y + 1;
        } else {
            set y;
        };
    };
    return 0;
}

fn INT_MAX() int {
    return 0;
}

fn largest(x: &a|int, y: &a|int) &a|int {
    if x +>= y {
        return x;
    } else {
        return y;
    }
}

fn swap(x: *a|int, y: *a|int) {
    let temp = ^x;
    ^x = ^y;
    ^y = temp;
}

fn test(x: &&b|*a|()) {
}
